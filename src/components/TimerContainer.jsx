const TimerContainer = ({children})=>{

    return (
        <div className="max-w-2x1 my-12 mx-auto flex flex-wrap gap-8 items-center justify-center">
            {children}
        </div>
    );
}

export default TimerContainer;